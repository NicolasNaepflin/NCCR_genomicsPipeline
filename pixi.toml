[workspace]
channels = ["conda-forge", "bioconda", "main", "r", "msys2"]
name = "nccrpipe"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = "3.12.*"
snakemake = ">=9.0"
snakemake-executor-plugin-cluster-generic = ">=1.0"
pyyaml = ">=6.0"
click = ">=8.0"
pandas = ">=2.0"
pytest = ">=7.0"
pytest-cov = "*"
pylint = "*"
pip = "*"
